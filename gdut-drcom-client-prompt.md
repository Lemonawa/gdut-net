# 任务：开发广东工业大学校园网第三方认证客户端（替代 Dr.COM 官方客户端）

## 背景与目标

学校（广东工业大学，大学城校区）有线网要求用 Dr.COM 官方客户端认证。官方客户端行为恶劣：
- 运行时扫描虚拟网卡（GetAdaptersInfo），发现"MAC 与物理网卡不一致的适配器"就动手，顶掉 Tailscale 的 wintun、Clash 的 TUN 网卡。检测节奏未摸清（至少登录时刻杀，可能持续或周期性扫描——被顶的适配器过一会"自己回来"可能只是 Tailscale 自愈），设计必须假设检测持续存在
- 捆绑尝试安装 Winsock LSP（TcpIpDog）+ WinPcap(npf.sys)，在 Win11 上驱动层装不上但桌面进程常驻，内存泄漏严重（旧版已知 >1GB）
- 无开机自启守护、断线需手动重连

目标：写一个第三方认证客户端，让我的 Windows 11 机器恢复以下体验：
1. Clash Verge 正常开 TUN 模式
2. Tailscale 开机即起、不被顶
3. 认证客户端本身开机自启、断线自动重拨、内存占用低（目标 <50MB）
4. 装机后无需人工干预

## 已确认的学校环境（2026-08-30 现场实测，可直接采信）

- 有线：**裸 PPPoE 直拨即可上网，无应用层心跳**。Windows 自带宽带连接，用户名=纯学号，密码=校园网密码。带 `\r\n` 前缀反而报 628（旧教程已过时）
- 无线：连 `gdut-X楼-房号` SSID 后浏览器网页认证（统一身份认证账号），无客户端
- 设备限制：1 有线 + 2 无线，靠 HTTP User-Agent + MAC 判定多设备；DHCP/随机 MAC 会误判超限
- 官方客户端在我机器上实际是"绿色"运行：LSP/npf 都没装成，只有桌面进程在做虚拟网卡检测。也就是说**认证服务器侧目前没有强制校验客户端心跳**——这正是第三方客户端的可行性基础
- 风险未知项：服务器侧未来可能开启心跳校验或客户端特征校验（Dr.COM 5.2.1p 协议家族有此能力），设计时留好开关

## 现有开源资产（先调研再动手，不要重复造轮子）

- drcoms/drcom-generic（Python，覆盖 d/p/x 三版，含 Dr.COM 协议分析与 keep-alive 抓包脚本）
- xnhw01/gdut-drcom（C，广工专用心跳库+CLI，Linux/Win mingw64，2023 年可编译）
- chenhaowen01/gdut-drcom-for-openwrt（OpenWrt ipk，逻辑可参考）
- GDUT-Drcom/Drcom-Dialer（C# Win GUI 广工拨号器，2019 停更，纯 PPPoE+心跳，无驱动）
- FUjr/gdut_network（2024，OpenWrt LuCI 插件，脚本 curl+jq 做网页认证自动登录，含 AC IP 探测：未登录时 `curl http://9.9.9.9` 从 NextURL 取 wlanacip）

## 需求规格

语言/形态：Rust 或 Go 单二进制，Windows 服务（或带托盘的常驻进程），源码 + GitHub Actions 构建
核心功能：
1. PPPoE 拨号管理：调用 Windows RAS（rasdial / RAS API）管理名为纯英文名（如 `gdut`）的拨号条目，断线检测 + 指数退避重拨
2. 兼容模式心跳（可配置开关，默认关）：可插拔加载 drcom-generic 或 gdut-drcom 的心跳逻辑，万一学校日后开启校验时启用。心跳必须绑定在物理网卡/IP 上发包
3. 不做任何虚拟网卡扫描、不装任何 LSP/驱动/WinPcap
4. 与 TUN 共存：一切网络操作显式绑定物理适配器（RAS 条目名），确保路由表被 Clash TUN/wintun 修改后重拨与心跳仍能正确出接口
5. 日志：滚动文件 + 可选事件日志，日志含认证结果/掉线原因
6. 配置：单个 TOML/YAML，字段含学号、密码（DPAPI 加密存储，不明文落盘）、拨号条目名、心跳开关与参数
7. 开机自启：以 Windows 服务方式启动，服务恢复选项配自动重启

明确非目标：不做 UI（托盘可选）、不做无线网页认证（那是浏览器的事）、不做限速绕过

## 验收标准

1. 全新 Win11 x64：安装服务 → 自动拨号成功 → 断网重连无需人工
2. 认证在线状态下开启 Clash TUN（虚拟网卡出现）与 Tailscale（wintun）：客户端不检测不干扰，重启后 TUN 环境下拨号重连依然成功
3. 任务管理器内存 <50MB，24 小时无泄漏
4. 关闭心跳（默认）时服务器侧连续 72h 不掉线（证明直拨成立）；模拟开启心跳时心跳包在物理接口上正确发出（抓包验证）
5. 卸载干净：服务、计划任务、注册表、日志全部移除

## 开发顺序建议

1. 先做 rasdial 封装 + 断线守护（这是 80% 价值的 20%）
2. 加 DPAPI 密码存储与配置
3. 加服务化 + 自启
4. 调研 drcom-generic 的 P 版心跳实现，做成可选模块（默认关闭，留好接口）
