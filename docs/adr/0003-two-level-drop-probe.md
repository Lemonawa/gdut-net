# 掉线判定采用两级探测，而非单看 RAS 状态

服务器踢号（多设备顶号）时 RAS 仍显示已连接、网关 ICMP 也仍通，单靠任一都判不出"僵死会话"。守护采用两级探测，均绑定物理适配器：网关 ICMP（默认 30s）探链路，异常或存疑时以 HTTP 探测（如 `http://9.9.9.9` 看 302）复核是否被踢，双失败才判掉线并触发重拨。探测参数可配置。

## Consequences

- 掉线判定有秒级延迟（一轮探测周期），换取不误判、不漏判。
- HTTP 探测目标须选不受 TUN 代理规则影响的直连地址，且显式绑物理适配器。
